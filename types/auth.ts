export type SignupCredentials = {
    fullname: string
    address: string
    email: string
    password: string
    role: string

}


export type LoginCredentials = {
    email: string
    password: string
}